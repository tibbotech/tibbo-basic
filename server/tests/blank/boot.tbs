include "global.tbh"


sub boot()
    dim i as byte
    for i = 0 to MAX_NUM_INTERFACES - 1
        sys.debugprint(str(i))
    next i

    dim f as byte
    for f=0 to MAX_NUM_INTERFACES
        interface_ready(f)=NO
    next f
    sys.debugprint("booting")
    
end sub